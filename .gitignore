# --- Source PDFs: kept LOCAL, never committed ---------------------------------
# Theses/dissertations are large binaries and (in part) third-party copyrighted.
# The sources registry (registries/sources.yaml) cites resolvable permalinks /
# DOIs instead of dumping the PDFs into history. See docs/decisions/0002-*.md.
sources/pdf/*
!sources/pdf/README.md

# Internal PAULA analysis notebooks (own primary sources): kept LOCAL and
# registered in registries/sources.yaml so records can cite them, but not
# committed (own unpublished analysis; poorly-diffable .nb blobs). See ADR-0002.
sources/Mathematica/*
!sources/Mathematica/README.md

# Raw measurement data (DAQ .dat blobs): kept LOCAL for now. The twins/engines
# read these when present and their tests skip when absent (e.g. twin_oc_flop).
# Re-include a dataset selectively once it ships with the analysis that cites it.
# (microwave/, MW_Control/, Tickle/ are already tracked and stay tracked.)
sources/data/Ion_Monitoring/
sources/data/OC_Axial/
sources/data/Strobo2.0/

# --- macOS --------------------------------------------------------------------
.DS_Store
**/.DS_Store

# --- Python -------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Editors / misc -----------------------------------------------------------
*.swp
.idea/
.vscode/
